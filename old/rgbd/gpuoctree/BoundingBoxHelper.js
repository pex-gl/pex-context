// Generated by CoffeeScript 1.6.2
var __hasProp = {}.hasOwnProperty,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

define(function(require) {
  var BoundingBoxHelper, Color, Context, Cube, Mesh, SolidColor, _ref;

  _ref = require('pex/gl'), Mesh = _ref.Mesh, Context = _ref.Context;
  Cube = require('pex/geom/gen').Cube;
  SolidColor = require('pex/materials').SolidColor;
  Color = require('pex/color').Color;
  return BoundingBoxHelper = (function(_super) {
    __extends(BoundingBoxHelper, _super);

    function BoundingBoxHelper(bbox, color) {
      var cube, size;

      if (!color) {
        color = Color.White;
      }
      size = bbox.getSize();
      cube = new Cube(size.x, size.y, size.z);
      cube.computeEdges();
      BoundingBoxHelper.__super__.constructor.call(this, cube, new SolidColor({
        color: color
      }), {
        useEdges: true,
        primitiveType: Context.currentContext.gl.LINES
      });
      this.position.copy(bbox.getCenter());
    }

    return BoundingBoxHelper;

  })(Mesh);
});
