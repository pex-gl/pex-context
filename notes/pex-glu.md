###pex-glu

- init
    ```
    glu.init();
    ```

- transformation
    ```
    glu.MODELVIEW_MATRIX;
    glu.PROJECTION_MATRIX;
    glu.setMatrixMode(mode);
    glu.getMatrixMode();
    glu.getProjectionMatrix(out);
    glu.getProjectionMatrixF32(out);
    glu.getViewMatrix(out);
    glu.getViewMatrixF32(out);
    glu.getModelViewMatrix(out);
    glu.getModelViewMatrixF32(out);
    glu.getNormalMatrix(out);
    glu.getNormalMatrixF32(out);
    glu.getProjectionMatrix(out);
    glu.getProjectionMatrixF32(out);
    glu.loadIdentity();
    glu.pushMatrix();
    glu.popMatrix();
    glu.pushMatrices();
    glu.popMatrices();
    glu.translate(vec);
    glu.translatef(x,y,z);
    glu.scale(vec);
    glu.scalef(x,y,z);
    glu.rotate(vec);
    glu.rotatef(x,y,z);
    glu.rotateAxis(angle,vec);
    glu.rotateAxisf(angle,x,y,z);
    glu.rotateQuat(quat);
    glu.multMatrix(mat);
    glu.setMatricesCamera(camera);
    glu.setMatricesWindow(width,height);
    ```
- draw
    ```
    glu.drawQuat(quat,length,offset,axos);
    glu.drawVector(v0,v1);
    glu.drawVectorf(x0,y0,z0,x1,y1,z1);
    glu.drawPoint(v);
    glu.drawPointf(x,y,z);
    glu.drawPoints(points);
    glu.drawPointsf(points);
    glu.drawLines(lines);
    glu.drawLinesf(lines);
    glu.drawLine(v0,v1);
    glu.drawLinef(x0,y0,z0,x1,y1,z1);
    glu.drawRect(width,height);
    glu.drawRectPoints(width,height);
    glu.drawRectStroked(width,height);
    glu.drawCircle(radius);
    glu.drawCircleStroked(radius);
    glu.drawCircles(positions,radii);
    glu.drawCirlcesStroked(positions,radii);
    glu.drawEllipse(radiusX,radiusY);
    glu.drawEllipseStroked(radiusX,radiusY);
    glu.drawEllipses(positions,radiiXY);
    glu.drawEllipsesStroked(position,radiiXY);
    glu.setCircleSegments(num);
    glu.getCircleSegements();
    glu.drawTriangle(v0,v1,v2);
    glu.drawTriangleStroked(v0,v1,v2);
    glu.drawTrianglePoints(v0,v1,v2);
    glu.drawCube();
    glu.drawCubePoints();
    glu.drawCubeStroked();
    glu.drawCubeColored();
    glu.drawSphere();
    glu.drawSpherePoints();
    glu.drawSphereStroked();
    glu.drwaSphereColored();
    glu.drawCylinder(base,top,height,numH,numV);
    glu.drawFrustum(frustum);
    glu.drawGrid();
    glu.drawGridPoints();
    glu.drawPivot(axisLength,headLength,headRadius);
    glu.drawOnB(onb);
    glu.drawMesh(mesh);
    glu.drawMeshes(meshes);
    glu.colorf(r,g,b,a);
    glu.color(color);
    glu.alpha(alpha);
    glu.getColor();
    glu.drawScreenRect(size,topleft);
    glu.drawScreenRectf(width,height,topleft);
    glu.drawWindowRect(topleft);
    ```
    
- state
    ```
    glu.STACK_OVERFLOW;
    glu.STACK_UNDERFLOW;
    glu.DRAW_LINEWIDTH_BIT;
    glu.DRAW_COLOR_BIT;
    glu.VIEWPORT_BIT;
    glu.CURRENT_BIT;
    ...
    glu.pushAttrib();
    glu.popAttrib();
    ```